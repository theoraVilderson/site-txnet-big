---
description: Match the backlog against code that already exists
---
Read `docs/00-PROTOCOL.md` and run MODE: RECONCILE. Report the table. Change
nothing until I approve. Be conservative — when unsure, mark `doing`, not `done`.
