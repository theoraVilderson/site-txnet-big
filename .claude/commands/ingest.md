---
description: Turn one catalog area into backlog rows
---
Read `docs/00-PROTOCOL.md` and run MODE: INGEST for area: $ARGUMENTS

Read `docs/features/MANIFEST.md` only, then `python3 tools/spec.py --area
$ARGUMENTS`. Never open the catalog file. Write BACKLOG rows only — no code, no
unit files, no catalog edits. Stop and ask if any feature has no home unit.
