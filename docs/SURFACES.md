---
id: surfaces
status: active
updated: 2026-09-09
code_roots:
  - site-pwa/src
  - coinsite/src
  - txnet-backend/auth-service/src
  - txnet-backend/bot-service/src
  - txnet-backend/worker-service/src
  - txnet-backend/messenger/src
  - txnet-backend/shared-core/src
  - auth-handler/internal
  - i18n-platform/services/locale-service
---

# Surface map — what the user can point at

`MASTER_INDEX.md` answers *"which unit owns this concern?"*. This file answers
the question a user actually asks: **"the register form on the landing site is
broken"** — a thing they can see, named in their own words, with no path
attached.

One row per user-visible surface: a screen, a tab, a button, a form, a bot
command, a public endpoint. Resolve a row with:

```bash
python3 tools/where.py "the register form on the landing site"
```

Never make the user say a path. If they had to, this file is missing a row.

## Rules

- `surface` — kebab-case, permanent, unique. It is an **id**, not a label.
- `aliases` — comma-separated, in the words a person actually types, including
  the sloppy short forms. This column is the whole point of the file; a row
  without aliases will never be found by a human's own phrasing. **Add an alias
  the moment a query misses.** Persian phrasing goes here too — `tools/where.py`
  normalises Persian/Arabic script.
- `unit` — must exist in `MASTER_INDEX.md`. A surface with no owning unit is a
  BLOCKING question (`00-PROTOCOL.md` §9), not a row to invent.
- `component` — a real path or glob, checked by `python3 tools/where.py --check`.
  This is the **only** place a UI path is written down.
- `spec ref` — the catalog id, never a line range. `—` if the surface predates
  the catalog.
- One surface = one thing a person can point a finger at. A page with six
  independent controls is one row for the page **plus** a row for each control
  that gets edited on its own.
- Never delete a row. A removed surface keeps its id and gets `(removed)` in the
  note, for the same reason a catalog id is never deleted.

## Panel (site-pwa)

| surface | aliases | route | unit | component | spec ref | note |
|---|---|---|---|---|---|---|
| panel-auth-proxy | auth proxy, login proxy, api auth proxy, register proxy, signup proxy |  | panel-web |  | — | (removed) 2026-09-05 — replaced by panel-auth-direct; browser calls auth-service cross-origin now instead of via a same-origin Next.js hop |
| panel-auth-direct | refresh token cookie, refresh_token cookie, کوکی رفرش توکن, کوکی ست نمیشه, مستقیم به api, direct api call, api.txnet.cyou مستقیم | `${NEXT_PUBLIC_API_ORIGIN}/api/auth/*` | panel-web | site-pwa/src/lib/auth-api.ts | — | browser -> auth-service direct, cross-origin, `credentials:"include"`; cookie lands via auth-api's CORS (`FRONTEND_ORIGIN` + `credentials:true`), not a proxy rewrite; see panel-web/contract.md TL;DR for the latency tradeoff this accepts |
| panel-form-error | error message, form error, ارور نشون نمیده, خطا نمایش داده نمیشه, پیام خطا, صفحه لاگین هیچی نمیگه, چرا هیچ خطایی نشون نمیده, the form does nothing when it fails | `/auth/login`, `/auth/register`, `/auth/forgot-password` | panel-web | site-pwa/src/app/(auth)/auth/_components/FormError.tsx | — | the one place an auth screen says a submit failed. The sentence comes from `auth-api` already translated; `useSubmitError` decides between it and `common.errors.unreachable` |
| panel-i18n-route | i18n route, translations endpoint | /api/i18n/[lang]/[ns] | panel-web | site-pwa/src/app/api/i18n | — | |
| panel-auth-screens | login screen, auth screens, login page | /(auth)/auth | panel-web | site-pwa/src/app/(auth)/auth | — | |
| panel-register-screen | register page, register screen, signup page, sign up screen, create account screen, صفحه ثبت‌نام, صفحه ثبت نام, صفحه signup, ساخت حساب کاربری, ثبت‌نام کاربر جدید | /auth/register | panel-web | site-pwa/src/app/(auth)/auth/register/page.tsx | — | renamed from `/auth/signup` on 2026-09-07 — auth-api (`POST /auth/register`), the bot (`RegisterFlow`) and coinsite (`/(Auth)/register`) always called it *register* and the panel was the only place calling it *signup*. `src/proxy.ts` 308s `/auth/signup*` to `/auth/register*` (path suffix + query preserved) so links already sent keep working; "signup" stays an alias here because that is still what a user says. Route constant: `AUTH_REGISTER` in `site-pwa/src/lib/routes.ts` |
| panel-nav | panel nav, top bar, header of the panel, نوار بالای پنل, nav پنل, هدر پنل | / | panel-web | site-pwa/src/app/(panel)/_components/PanelNav.tsx | — | the signed-in counterpart of `AuthNav`; lives in the `(panel)` route group layout, so every future panel page gets it |
| panel-logout-button | logout, log out, sign out, دکمه لاگ اوت, خروج از حساب, لاگ اوت | / | panel-web | site-pwa/src/app/(panel)/_components/LogoutButton.tsx | — | POSTs `/auth/logout` (the refresh cookie is the credential, so it works after a reload with no access token in memory) then `router.replace` to the login screen; a failed call still leaves — the device is signed out either way |
| panel-account-switcher | account switcher, switch account in the panel, change account, my accounts menu, سوییچر حساب, تعویض حساب تو پنل, منوی حساب ها, چند اکانته رو کجا میبینم, چطور اکانت عوض کنم | / (in the panel nav) | panel-web | site-pwa/src/app/(panel)/_components/AccountSwitcher.tsx | F-0209 | current account + the rest of the group + "add an account". A switch does a full `window.location` navigation, not `router.push`: everything already fetched belongs to the account being left, and its session is revoked by the call. **The set is this browser's alone** (ADR-0015) — partitioned by a server-minted httpOnly `device_id` cookie, so another browser starts empty and a chat sees its own. Also holds the remove control (`panel-account-remove`) |
| panel-add-account | add an account page, add second account in the panel, صفحه افزودن حساب, اضافه کردن حساب دوم, حساب دیگه اضافه کنم | /accounts/add | panel-web | site-pwa/src/app/(panel)/accounts/add/page.tsx | F-0205 | two tabs = the two proofs the API accepts (code to that account's phone, or that account's password). A `linkRequired` answer inserts the connect-the-bot step, same as login (F-0203). What is added joins the group **of the browser it was added from** (ADR-0015), not the person globally. Its two password-tab inputs carry placeholders, not floating labels, so nothing visibly moves here — but Confirm is disabled off their state, and an autofill React never hears about leaves it disabled under a form that looks filled in. They take `_hooks/useAutofill.ts` for that reason, and `.autofill-tamed` so the browser's yellow fill background does not land on a dark panel |
| panel-account-remove | remove an account, remove account from the switcher, unlink my other account, leave the account group, حذف حساب از سوییچر, حذف اکانت از گروه, اکانت اشتباهی اضافه کردم, چطور اکانت رو از لیست پاک کنم, میخوام از گروه حساب ها خارج شم | / (in the account switcher) | panel-web | site-pwa/src/app/(panel)/_components/AccountSwitcher.tsx | F-0208 | the trash control on a member row, and "remove this account" on the current one — F-0208 works from either side. Confirms inline (the row becomes the question) rather than with `window.confirm`, because the control sits one tap from the one that *switches* to the same account. Removes from **this browser only** (ADR-0015); removing the current account revokes this browser's session, so that path reloads instead of re-reading the group |
| panel-session | who am i signed in as, current account in the panel, panel session, پنل نمیدونه کی لاگینه, بعد از رفرش لاگ اوت میشم تو پنل | (every panel page) | panel-web | site-pwa/src/app/(panel)/_context/PanelSessionContext.tsx | F-0209 | a page load has no access token in memory, so this trades the httpOnly refresh cookie for one — **once** per load (`authApi.ensureSession`), because refresh rotates and a second concurrent call signs the loser out. No live session -> `replace` to the login screen, the mirror of `panel-auth-session-redirect` |
| panel-home | panel home, panel root page, صفحه اصلی پنل, روت پنل | / | panel-web | site-pwa/src/app/(panel)/page.tsx | F-0209 | shows which account this browser is and the rest of the group — the only thing on the panel a switch visibly changes today |
| panel-forgot-password | forgot password, forgot password page, reset password screen, فراموشی رمز, رمزمو فراموش کردم, بازیابی رمز عبور, صفحه فراموشی رمز | /(auth)/auth/forgot-password | panel-web | site-pwa/src/app/(auth)/auth/forgot-password/page.tsx | F-0204 | four steps: phone + delivery method -> (connect messenger, if needed) -> code -> new password. On success the device is signed in and lands on `PANEL_HOME`; every other device is signed out |
| panel-otp-channel-picker | otp channel picker, delivery method, how to receive the code, choose sms or telegram, انتخاب روش ارسال کد, کد رو با تلگرام بفرست, انتخاب پیامک یا تلگرام یا بله | (embedded in login + forgot-password) | panel-web | site-pwa/src/app/(auth)/auth/_components/OtpChannelPicker.tsx | F-0202 | renders only the channels `GET /auth/otp/channels` returns (`_hooks/useOtpChannels.ts`); hidden when there is only one |
| panel-bot-link-step | connect telegram, connect bale, share contact with bot, open bot link, اتصال تلگرام, اتصال بله, ارسال مخاطب به ربات, لینک ربات باز نمیشه | (step in login + forgot-password) | panel-web | site-pwa/src/app/(auth)/auth/_components/BotLinkStep.tsx | F-0203 | shows the `?start=` deep link and polls `POST /auth/bots/link/status` every 2.5s (`_hooks/useBotLink.ts`); the code is sent by the bot, not by this screen |
| panel-auth-session-redirect | redirect if already logged in, token valid redirect, اگه توکن کاربر valid بود ریدارکت بشه, ریدارکت به panel.txnet.cyou, بعد از لاگین کجا بره, نره به /dashboard, سر روت پنل, panel root redirect, login page while signed in, ریدارکت سریع, تشخیص سریع لاگین, refresh token نامعتبر پاک بشه, clear invalid refresh token, next.js proxy redirect, middleware redirect | (server-side, on /auth/login and /auth/register) | panel-web | site-pwa/src/proxy.ts | F-0101 | runs in the Next 16 proxy (middleware), before the screen renders: no `refresh_token` cookie -> straight through, no request; cookie -> `GET /api/auth/session` server-to-server at `AUTH_SERVICE_ORIGIN`, `data.active` -> 307 to `PANEL_HOME`, anything else -> the form. auth-service's `Set-Cookie` is forwarded verbatim, so a dead token's clear reaches the browser. **It asked `POST /auth/refresh` until 2026-09-06 and that was the bug** — refresh rotates, this handler runs on every non-static path including RSC prefetches, and a rotation whose `Set-Cookie` the browser dropped left a revoked cookie in place (ADR-0013, flow `panel-login-bounce-rotation`). Every failure (unreachable, timeout, bad body) ends at the form. `PANEL_HOME` is `/` in `site-pwa/src/lib/routes.ts`, relative so white-label tenants keep their own host. Replaced the old `/dashboard` push, a route this app never had. `forgot-password` is intentionally not guarded |
| panel-locale-store | translations on the client, locale store, کش ترجمه‌ها, namespace injector, addNamespaces, locale-store.ts, تزریق namespace, تغییر زبان در پنل, زبان سمت کلاینت, client side language switch | (every auth and panel screen) | panel-web | site-pwa/src/stores/locale-store.ts | — | one zustand store per `LocaleProvider` mount, seeded on the server with every language's namespaces so `setLang` needs no fetch. `useLocale()` subscribes to `cache`, so anything that calls `addNamespaces` must do it in an effect, never during render. `LocaleContext.tsx` holds the provider + `useLocale`, `components/NamespaceInjector.tsx` the per-route injection — see the `panel-locale-namespace-injection` flow |
| panel-captcha-widget | captcha, slide captcha, human verification, bot check, کپچا, تایید ربات, اسلایدر کپچا | (embedded in login/register/forgot-password) | panel-web | site-pwa/src/app/(auth)/auth/_components/NatureCaptchaUI.tsx | F-0201 | driven by `_hooks/useCaptcha.ts` |
| panel-phone-field | phone field, phone number input, country picker, country code, dial code, choose country, فیلد شماره, ورودی شماره موبایل, انتخاب کشور, کد کشور, پیش‌شماره کشور, شماره خارجی قبول نمیکنه, شماره غیر ایرانی, شماره کشور دیگه وارد کنم, دکمه پرچم نیست, انتخاب کشور بالا نمیاد, پیش شماره بالا نمیاد, کشور رو نمیشه عوض کرد, شماره برای کشور های دیگه, فرمت بومی شماره, فرمت رسمی شماره, شماره بین المللی تایپ کنم | (embedded in login/register/forgot-password + add-account) | panel-web | site-pwa/src/app/(auth)/auth/_components/PhoneField.tsx | — | a country selector plus a national number, submitting E.164 (ADR-0018), over `src/lib/phone.ts`. Every country the library knows is offered, named in the panel's own language via `Intl.DisplayNames` and flagged from the ISO code — no country list is shipped. It opens on the country the **deployment's** default language implies (`defaultPhoneCountry`), not the browser's, and `NEXT_PUBLIC_DEFAULT_PHONE_COUNTRY` overrides that. Autofill of the national part goes through `_hooks/useAutofill.ts` like the other two fields — the replayed `input` event runs the same `formatAsTyped`/`toE164` path a keystroke does, so an autofilled number is still submitted as E.164. **Two things about it are load-bearing and easy to undo.** `.field-nature` is a *positioned* element with an opaque `--bg-inner` background sitting at `inset: 0`, so it paints over every non-positioned sibling in the field — the input escapes with `position: relative; z-index: 2`, and the country button needs the same (`relative z-[2]`) or it is invisible and unclickable, which is exactly what it looked like: a plain number box with no picker at all (2026-09-08). And `.organic-field label` is positioned against the whole field, so in a field that is a *row* the resting label lands on the button — the input and its label live in their own `relative flex-1` wrapper so the label is measured from the input's box. Any future field built out of `.organic-field` plus a second control needs both. **Either spelling of a number is accepted** (`readTyped` in `src/lib/phone.ts`): a national number stays with the picker's country, while one written internationally (`+49…`) or with the selected country's own IDD prefix (`0049…`) moves the picker to its country and has its dial code lifted onto the button, so the field never shows `+49` twice |
| panel-text-field | username field, text field, floating label, label doesn't move, autocomplete username, یوزنیم فیلدش پر میشه ولی لیبل ها تکم نمیخورن, یوزرنیم فیلدش پر میشه ولی لیبل ها تکون نمیخورن, لیبل تکون نمیخوره, فیلد پر میشه لیبل بالا نمیره, اتوکامپلیت یوزرنیم, خط نازک پیش فیلد نام کاربری, یه خط نازک ماله اتوفیل هستش, بعد اتوفیل یه خط نازک میمونه, کادر فیلد بعد اتوفیل ناقص میشه, autofill leaves a thin line, autofill border artifact | (embedded in login/register/forgot-password + add-account) | panel-web | site-pwa/src/app/(auth)/auth/_components/OrganicField.tsx | — | the plain floating-label input (username, email, name). Its label and the submit button both read React `value`, so an autofill React never hears about leaves the field *looking* full, the label down and the button disabled — `useAutofill` is what closes that, shared with `panel-password-field` and `panel-phone-field`. **But JS cannot close all of it**: while Chrome's fill is still a *preview* (2026-09-08 screenshot — the value painted straight across the resting label on a page still loading) the text is drawn and nothing else is true — `value` is `""`, `:placeholder-shown` still matches, hydration has not run. Only `:autofill` / `:-webkit-autofill` match from the first paint, so the label's float has a CSS-only path in `globals.css` next to the JS one. Anything that renders a floating label over an autofillable input needs both halves — and any field whose submit button reads its state needs the JS half even with no label at all (`panel-add-account`). The hook holds its own callback ref so a field behind a tab or a step is watched from when it mounts, not from when its parent did. **The field's border is not on the input** — it is on the `.field-nature` sibling at `inset: 0`, i.e. exactly the input's own box. Autofill is painted by the browser *on the input*, so the opaque `box-shadow: … inset` that tames its background also covers that 1.5px border along every straight edge and leaves only the arcs of the fill's own (smaller) corner radius showing: the thin line an autofilled field shows and an empty one does not (2026-09-08). The input now carries a 1.5px transparent border of its own — an inset shadow is clipped to the padding box, so the fill stops at the border instead of on it — with the padding reduced by the same 1.5px and no radius override on the autofill rule. Any new autofill rule here must not re-introduce a radius that differs from `.field-nature`'s |
| panel-password-field | password field, password label, floating label, autocomplete پسورد, پسورد لیبل بالا نمیره, اتوکامپلیت پسورد لیبل بالا نمیره, پسورد پر میشه خیلی دیر لیبلش میره بالا, لیبل دیر میره بالا, موقع لود صفحه auth لیبل ها درست نیستن | (embedded in login/register/forgot-password) | panel-web | site-pwa/src/app/(auth)/auth/_components/PasswordField.tsx | — | label float relied on React `value` state; browser/password-manager autofill doesn't fire onChange, so the label stayed down until a manual click. First patched 2026-09-05 with `:-webkit-autofill` CSS-animation detection, **which is not enough on its own** — the marker animation fires before hydration attaches the handler, and a one-shot `matches()` on mount misses a fill that lands a beat later. 2026-09-08: `_hooks/useAutofill.ts` polls a frame at a time for a short window *and* replays the DOM value as an `input` event, so React's state catches up too — the label was only the visible half of that bug. The pre-hydration preview state needs CSS instead (see `panel-text-field`) — here the float lives in framer-motion's inline style and the label precedes the input, so it takes a `:has()` rule with `!important`, pinned to the values motion animates to so the handover is invisible |

## Bot (Telegram + Bale)

One row per screen the user can name inside the bot. Both messengers render the
same `BotView`, so a row is never per-platform — if something differs, it is a
`messenger` capability, not a second surface.

| surface | aliases | route | unit | component | spec ref | note |
|---|---|---|---|---|---|---|
| bot-webhook | bot webhook, ربات جواب نمیده, وبهوک ربات, bot url, telegram webhook, bale webhook, webhook path, وبهوک پث | POST /api/bots/:platform/:webhookPath | bot-app | txnet-backend/bot-service/src/app/webhook/webhook.controller.ts | F-320 | one unguessable path per **bot**, and the path is what resolves the tenant — never the message body. An unknown path, a wrong secret token, or a Telegram request with no secret header is a bare 404; a known path always answers 200. Registered on boot by `BotWebhookRegistrar`, per `BotIntegration` row |
| bot-menu | bot menu, /start, منوی ربات, استارت ربات, ربات رو باز کردم چی نشون میده | /start | bot-app | txnet-backend/bot-service/src/app/flows/views.ts | F-303 | guest menu vs member menu, decided by the chat's Redis session — never by the chat id |
| bot-login | login in the bot, ورود در ربات, لاگین تو بات, ورود با کد در ربات, ورود با رمز در ربات | (menu -> Sign in) | bot-app | txnet-backend/bot-service/src/app/flows/login.flow.ts | F-303 | OTP or password, the same two ways the panel offers |
| bot-register | register in the bot, ثبت نام در ربات, ساخت حساب تو بات, ثبت نام تلگرام, ثبت نام بله | (menu -> Create an account) | bot-app | txnet-backend/bot-service/src/app/flows/register.flow.ts | F-303 | contact -> name -> username -> channel -> password; the password message is deleted the moment it is used |
| bot-forgot-password | forgot password in the bot, فراموشی رمز در ربات, بازیابی رمز تو بات | (menu -> I forgot my password) | bot-app | txnet-backend/bot-service/src/app/flows/forgot.flow.ts | F-303 | reset signs every other device out and keeps this chat signed in |
| bot-otp-channel-picker | where should the code go, otp channel in the bot, کد کجا بیاد, انتخاب پیامک یا تلگرام یا بله در ربات, otp بله داخل تلگرام, otp تلگرام داخل بله | (step in every bot auth flow) | bot-app | txnet-backend/bot-service/src/app/flows/otp.step.ts | F-303 | this chat first, then SMS, then the other messenger. Choosing the other messenger hands over its deep link; choosing this one links in place — except when the code is not for the person in this chat (`bot-add-account` passes `inPlace: false` and always gets the deep link) |
| bot-phone-step | the bot asks for my number, phone step in the bot, share contact with the bot, number from another country in the bot, foreign number in the bot, شماره در ربات, ربات شماره میخواد, شماره‌ام رو به ربات بدم, شماره خارجی در ربات, شماره غیر ایرانی تو بات, ربات شماره خارجی رو قبول نمیکنه, کد کشور در ربات | (step in every bot auth flow) | bot-app | txnet-backend/bot-service/src/app/flows/phone-number.ts | F-062 | typed or shared as a contact, then read into E.164 the way `panel-phone-field` does. A number that names its own country (`+49…`, `0049…`, or a contact whose `+` the messenger dropped) is kept; a bare national number is read as the deployment's region (`DEFAULT_PHONE_COUNTRY`, else the bot's language). There is no country picker in the chat — an unreadable answer travels on untouched and comes back as `auth-api`'s own refusal |
| bot-accounts | my accounts in the bot, switch account in the bot, حساب های من در ربات, سوییچ اکانت در ربات, تعویض حساب در ربات, جابه جایی بین حساب ها در ربات, عوض کردن اکانت تو بات, چند اکانته در ربات, این قابلیت switch اکانت رو داخل روبات ها هم داشته باشه | (menu -> My accounts) | bot-app | txnet-backend/bot-service/src/app/flows/accounts.flow.ts | F-0210 | the panel's switcher (`F-0209`) on a chat: the same `GET /auth/accounts` + `POST /auth/accounts/switch`, no credential. **The session moves, the link does not** (ADR-0014) — so after `/logout` the one-tap sign-in returns to the *linked* account, not the one last switched to. "Add an account" is the last row on both versions of this screen and opens `bot-add-account`. Reached only from the member menu. **The set belongs to this chat** (ADR-0015): every call carries `x-bot-platform` beside `x-bot-chat-id`, and the browser's set is neither visible nor changeable from here. "Remove an account" (`F-0208`) is the row below add — pick, confirm, done; removing this chat's own account signs it out here and drops the stored refresh token |
| bot-add-account | add an account in the bot, add a second account in the bot, افزودن حساب در ربات, اضافه کردن حساب تو ربات, حساب دوم در ربات, افزودن یک حساب تازه به گروه, اضافه کردن اکانت جدید تو بات, اثبات حساب دوم با کد یا رمز, سوییچ خودکار بعد از افزودن حساب, بعد از اضافه کردن حساب سوییچ نمیشه, حساب اضافه شد ولی وارد نشدم, حساب رو اضافه کردم ولی هنوز با اکانت قبلی هستم | (menu -> My accounts -> Add an account) | bot-app | txnet-backend/bot-service/src/app/flows/account-add.flow.ts | F-0205 | the panel's add-account page (`panel-add-account`) as a conversation: pick a proof, then a code to the joining account's own phone or that account's own password. **A successful add lands the chat on the new account.** The add itself mints nothing — it answers `userId` (auth-api contract v8) and the flow spends the same access token on an ordinary `POST /auth/accounts/switch`, through `session/account-switcher.ts`. A refused switch is not an error: the add stands, and the chat stays where it was with the plain "it is in your list" message. **The code goes to somebody else's number**, so this is the one flow that asks `OtpStep` for `inPlace: false`: linking the joining account here would move this chat's own link, and identity refuses it anyway (`takenByAnotherAccount`, invariant #12). It joins **this chat's** group only (ADR-0015) |
| bot-remove-account | remove an account in the bot, حذف حساب در ربات, حذف اکانت از ربات, پاک کردن حساب از لیست ربات, از گروه حساب ها خارج شدن در ربات, اکانت رو از بات حذف کن | (menu -> My accounts -> Remove an account) | bot-app | txnet-backend/bot-service/src/app/flows/accounts.flow.ts | F-0208 | pick a member, confirm, done — `drop:<userId>` is a second action prefix beside `account:<userId>` so a tap can never mean "switch" and "remove" depending on remembered state. The list is re-read before the question *and* the confirm, so a stale keyboard cannot remove whoever has since taken that row. Removing this chat's own account signs it out here and clears `bot:session:*` — auth-api has already revoked the token behind it |
| bot-mini-app | open the app, mini app, miniapp, webapp, بازکردن اپ, باز کردن اپ در ربات, مینی اپ, مینی‌اپ, پنل داخل تلگرام, پنل داخل بله, پنل توی ربات باز شه, وب اپ, دکمه باز کردن اپ, panel inside telegram, panel inside bale, open the panel in the bot, mini app asks me to log in, مینی اپ ازم لاگین میخواد, داخل مینی اپ لاگین میخواد | (menu -> Open the app) | bot-app | txnet-backend/bot-service/src/app/flows/views.ts | F-310 | the panel itself inside the messenger — never a third UI (ADR-0009). One `kind: 'web_app'` row on the **member** menu, present only when `PANEL_BASE_URL` is set; a platform without the WebApp surface gets the same URL as a plain link (the renderer's degradation). The page signs itself in: the messenger hands it a signed `initData`, `PanelSessionProvider` presents that to `POST /auth/bots/webapp/session` **after** the refresh cookie fails, and the session that comes back is the ordinary one under the *browser's* switch scope (ADR-0017). **Being asked to log in inside the Mini App is a real answer, not a bug**: either the signature did not verify, or that messenger account has never shared its contact card with the bot (`needsContact`) — a Mini App cannot ask for one, so the chat is where that is fixed. The other two files this crosses: `site-pwa/src/lib/mini-app.ts` (the whole per-platform surface the panel has) and `bot-link.controller.ts` (`webapp/session`) |
| bot-logout | logout in the bot, /logout, خروج از ربات, لاگ اوت تو بات | /logout | bot-app | txnet-backend/bot-service/src/app/conversation/router.ts | F-303 | revokes the session at `auth-api`, then drops the chat's Redis entry |

## Marketing (coinsite)

| surface | aliases | route | unit | component | spec ref | note |
|---|---|---|---|---|---|---|
| marketing-login | landing login, login page on landing site | /(Auth)/login | marketing-web | coinsite/src/app/(Auth)/login | — | |
| marketing-register | landing register, signup page on landing site | /(Auth)/register | marketing-web | coinsite/src/app/(Auth)/register | — | |

## API

| surface | aliases | route | unit | component | spec ref | note |
|---|---|---|---|---|---|---|
| auth-api-register | register endpoint | POST /auth/register | auth-api | txnet-backend/auth-service/src/app/auth | — | |
| auth-api-login | login endpoint, password login, sign in with username and password, ورود با نام کاربری و پسورد, لاگین با یوزرنیم و رمز, ورود با رمز عبور, لاگین پنل, لاگین سایت | POST /auth/login/password | auth-api | txnet-backend/auth-service/src/app/auth/auth.service.ts | — | `loginWithPassword`. Answer order is invariant #6 / rule 12 — see the `login-invalid-credentials` flow before assuming a bug |
| auth-api-impersonate | impersonation, impersonate user | POST /admin/users/:userId/impersonate | auth-api | txnet-backend/auth-service/src/app/impersonation | — | |
| auth-api-bot-webhook-rotate | rotate the bot webhook path, change the bot webhook url, the bot webhook path leaked, kill the old webhook path, re-register the bot webhook, چرخش مسیر وبهوک ربات, عوض کردن آدرس وبهوک, مسیر وبهوک ربات لو رفته | POST /admin/bots/:platform/:botUsername/webhook/rotate | messenger | txnet-backend/auth-service/src/app/automation/webhook-rotation.service.ts | F-322 | needs `bot.webhook_rotate`. The bot is named by its `@handle` within the request's own tenant, never by its path. The old path stops resolving before the platform is called, so `registered:false` is a quiet bot, never one still listening on a burned address (`messenger/contract.webhook.md`) |
| admin-worker-list | list the background workers, which workers exist, worker registry, show the cron jobs, background jobs page, when did that job last run, لیست ورکرها, کارهای پس‌زمینه, ورکر کی اجرا شد | GET /admin/workers | automation | txnet-backend/auth-service/src/app/automation/worker-admin.service.ts | — | needs `worker.manage`. Each schedule answers a `shapeError`; non-null means that row could never run (automation invariant #2), which is the only way a schedule typed straight into the database becomes visible |
| admin-worker-schedule | schedule a worker, set a cron for a job, run this job nightly, give the worker a time window, add a schedule, زمان‌بندی ورکر, کران برای جاب, بازه زمانی ورکر | POST /admin/workers/:key/schedules, PATCH /admin/workers/:key/schedules/:scheduleId | automation | txnet-backend/auth-service/src/app/automation/worker-admin.service.ts | — | needs `worker.manage`. The three shapes are mutually exclusive and a bad one is refused with the rule it broke, by the same `scheduleShapeError` the tick publisher declines on (`shared-core/src/lib/automation/schedule.ts`). Nothing is deleted — a schedule is switched off |
| admin-worker-toggle | switch a worker off, turn off the campaign sender, disable a background job, kill switch for a worker, stop that job from running, ورکر رو خاموش کن, غیرفعال کردن جاب پس‌زمینه | PATCH /admin/workers/:key | automation | txnet-backend/auth-service/src/app/automation/worker-admin.service.ts | — | needs `worker.manage`. Automation invariant #1 — the fastest kill switch, audited as `bot_toggle` in the same transaction. A tick already on the queue still runs; the switch is a publish-side gate, not an undo |
| admin-worker-run-now | run this worker now, trigger the job manually, run the job on demand, force a worker run, همین حالا اجرا کن, اجرای دستی ورکر | POST /admin/workers/:key/run | automation | txnet-backend/auth-service/src/app/automation/manual-tick.publisher.ts | — | needs `worker.manage`. Publishes an `admin_manual` tick — 200 means *asked for*, and the outcome is a `bot_execution_log` row written by `worker-service`. Bypasses the schedule, never `isActive`. 503 when `RABBITMQ_URL` is unset or the broker is down |
| auth-api-captcha | captcha endpoint, human verification api, bot check api, کپچا, تایید ربات | POST /auth/captcha/challenge, POST /auth/captcha/verify | auth-api | txnet-backend/auth-service/src/app/auth/captcha | F-0201 | required (`X-Captcha-Token`) on register/login/forgot |
| auth-api-otp-channels | otp channels endpoint, available delivery methods, which otp channels are on, اندپوینت کانال های otp, روش های ارسال کد | GET /auth/otp/channels | auth-api | txnet-backend/auth-service/src/app/auth/otp/otp-channels.service.ts | F-0202 | env-driven (`OTP_ALLOWED_CHANNELS` + configured senders) |
| auth-api-bot-link-status | bot link status, is the messenger linked yet, وضعیت اتصال ربات | POST /auth/bots/link/status | auth-api | txnet-backend/auth-service/src/app/auth/bot-link/bot-link.controller.ts | F-0203 | polled by `panel-bot-link-step` |
| auth-api-account-switch-add | add another account, add a second account, link my other account, account switch group, حساب دوم اضافه کن, اضافه کردن اکانت دیگه, گروه حساب ها, چند اکانت, چند اکانته, جابه جایی بین اکانت ها, سوییچ اکانت, multi account, switch account, چند اکانته رو چطور تست کنم | POST /auth/accounts/add/otp/request, /auth/accounts/add/otp/verify, /auth/accounts/add/password | audit | txnet-backend/auth-service/src/app/account-switch/account-switch.controller.ts | F-0205 | behind `AuthGuard`, deliberately **not** behind `NoActiveSessionGuard` — a live session is the premise, not an obstacle (C-21). Two proofs: an OTP to the joining account's own phone (`OtpPurpose.account_switch_link`, its own purpose so it can never be spent as a login) or that account's password, which consumes the same `login-failures` bucket. Every failed proof answers the one key `accountSwitch.proofFailed`; a call whose surface cannot be identified answers `accountSwitch.noScope` and spends no proof (ADR-0015) |
| auth-api-account-switch-list | list my accounts, my account group, which accounts can i switch to, لیست حساب هام, گروه حساب ها رو بده, حساب های لینک شده | GET /auth/accounts | audit | txnet-backend/auth-service/src/app/account-switch/account-switch.service.ts | F-0206 | `{groupId, current, members}`; phone always masked (`0912***6789`) and members filtered to the caller's own tenant (C-22, audit invariant #6). No group yet answers `members: []`, not an error — and since ADR-0015 that is also the ordinary answer on a surface where no group has been built yet, because the read is scoped by `device_id` cookie or `x-bot-platform` + `x-bot-chat-id` |
| auth-api-account-switch | switch account, change account, become my other account, سوییچ کن به حساب دیگه, عوض کردن حساب, تعویض اکانت بدون رمز | POST /auth/accounts/switch | audit | txnet-backend/auth-service/src/app/account-switch/account-switch.controller.ts | F-0207 | body is `{userId}` and nothing else — no credential, which is the point of the group. Issues the target's token pair + `refresh_token` cookie and revokes the caller's session `account_switched`, in one transaction (audit invariant #7, C-21). Not in the group **of this scope**, another group, another tenant, deleted or suspended, or no resolvable scope all answer `accountSwitch.notAMember` (ADR-0015) |
| auth-api-account-switch-remove | remove an account from the group, unlink an account, leave the switch group, endpoint حذف حساب از گروه, حذف عضو از گروه حساب ها, api حذف اکانت لینک شده | POST /auth/accounts/remove | audit | txnet-backend/auth-service/src/app/account-switch/account-switch.controller.ts | F-0208 | body is `{userId}`, which may be the caller's own — F-0208 is "from either side". Drops the `(scopeKey, userId)` row and revokes that account's sessions **in that scope only** (`account_unlinked`), never globally: since ADR-0015 a browser has no authority over a chat's group. Deletes the group once one member would be left. Mints nothing and sets no cookie, so a self-removal is a sign-out |
| auth-api-no-active-session-guard | block relogin with valid token, prevent signup while logged in, گارد جلوگیری از لاگین دوباره, توکن معتبر نتونه دوباره لاگین کنه, نتونه دوباره ثبت‌نام کنه | POST /auth/register, /auth/login/password, /auth/login/otp/request, /auth/login/otp/verify | auth-api | txnet-backend/auth-service/src/app/auth/guards/no-active-session.guard.ts | F-0101 | 409 `auth.alreadyAuthenticated` if Bearer token verifies to a live session; logout clears it; backend only, no panel-web UI yet |
| forward-auth-rbac-policy | rbac, permissions file, policy file, role permissions, permission denied by the gateway, 403 from traefik, نقش دسترسی نداره, فایل پرمیشن, موتور نقش‌ها, engine.go, policy.go | POST /validate (Traefik ForwardAuth) | forward-auth | auth-handler/internal/auth | — | the `roles: -> <role>: -> permissions:` indent format in `configs/permissions.yaml` (`PERMISSIONS_FILE_PATH`), parsed at boot by `LoadFile` and enforced per request by `Engine.Check`. A malformed file is a hard boot error, never a partial policy |

## Bot / other

| surface | aliases | route | unit | component | spec ref | note |
|---|---|---|---|---|---|---|
| bot-link-webhook | telegram webhook, bale webhook, bot webhook, ربات جواب نمیده, وبهوک تلگرام, وبهوک بله, ربات مخاطب رو قبول نمیکنه | POST /auth/bots/:platform/webhook/:secret | auth-api | txnet-backend/auth-service/src/app/auth/bot-link/bot-link.controller.ts | F-0203 | the bot's `/start <token>` + shared-contact conversation. Wrong/absent secret -> 404. Handling is best-effort and always answers 200, so the platform never redelivers |
| bot-otp-delivery | otp in telegram, code in bale, ربات کد نمیفرسته, کد تو تلگرام نمیاد | (outgoing sendMessage) | identity | txnet-backend/auth-service/src/app/auth/otp/senders/ | F-0202 | only delivers to a `linked_bot_account` with `contactVerifiedAt` set |

## Flows

A surface is a thing you point at. A flow is a **behaviour that crosses units**:
a cookie login touches `panel-web`, `auth-api` and `redis-keyspace`.
`python3 tools/where.py --walk "<sentence>"` derives that chain from
`depends_on` plus the runtime edges in
`architecture/dependency-graph.md` every time it is asked. This table is the
cache.

**Never write a flow row in advance.** A row written before the walk is a guess
at which files matter, and it is wrong in the most expensive way — it looks like
knowledge. Rows are written **after** a fix, from the path actually taken, by
MODE: DIAGNOSE (`00-PROTOCOL.md` §6g) step 7. The second time the same thing is
reported, there is no walk at all.

- `flow` — kebab-case, permanent, unique. An id, like a surface.
- `aliases` — **the user's own sentence, verbatim**, symptom words included,
  Persian phrasing included. That is the row's whole value.
- `path` — the unit chain, `->` separated, in the order the walk took it. Every
  id must exist; `where.py --check` fails otherwise.
- `files` — only the files the fix actually touched, not the files that were
  read. A flow row is evidence, not a reading list.
- Never delete a row. A flow that stops existing keeps its id and gets
  `(removed)` in the note.

| flow | aliases | path | files | spec ref | note |
|---|---|---|---|---|---|
| login-invalid-credentials | auth.invalidCredentials, invalid credentials, ورود نمیشه, لاگین نمیشه, داشتم با نام کاربری و پسورد ورود میزدم ارور گرفتم, ارور موقع لاگین, رمزم درسته ولی نمیذاره وارد شم, Console Error auth-api.ts request, لاگین ارور میده | panel-web -> auth-api -> identity -> redis-keyspace | txnet-backend/auth-service/src/app/auth/auth.service.ts | — | **No fix — the behaviour is correct.** The file named is where the branch lives, not a change. `auth.invalidCredentials` deliberately means three different things (rule 12): no such account, account deleted/inactive, or wrong password. They are indistinguishable from outside on purpose — that is the account-existence oracle invariant #6 closes. From inside, one Redis key separates them: `AuthService.loginWithPassword` writes `ratelimit:login-failures:<normalized identifier>` **after** finding a live account and **before** `argon2.verify`. So: key exists -> the account was found and the password was wrong; key absent -> the identifier matched no live account. Read it with `docker exec txnet-dev-redis redis-cli -a "$REDIS_PASSWORD" --scan --pattern '*login-failures*'`. Nothing is logged on this path, so the service log is empty either way — do not read that as the request never arriving. Sharp edge (rule 11): the counter is consumed *before* the password check, so the 11th attempt in a 900s window is locked even when its password is right — send the user to forgot-password rather than let them keep guessing. First diagnosed 2026-09-06; `--walk` sent this sentence to `bot-app`, fixed by giving `auth-api-login` real aliases |
| forgot-password-otp | forgot password, forgot password with otp, رمزمو فراموش کردم, بازیابی رمز با کد, otp برای فراموشی رمز, کد بازیابی نمیاد, بله رو انتخاب کردم ارور داد, Something went wrong روی password/forgot | panel-web -> auth-api -> identity -> redis-keyspace | site-pwa/src/app/(auth)/auth/forgot-password/page.tsx, txnet-backend/auth-service/src/app/auth/auth.service.ts, txnet-backend/auth-service/src/app/auth/otp/otp.service.ts | F-0204 | `password/forgot` -> `password/forgot/verify-otp` -> `password/reset`. Redis holds the code; the reset revokes every session then issues one for this device. A generic `{ok:false,ref}` here is never the channel — take the `ref` to the auth-service log, the real cause is above it (2026-09-05: dev DB behind `identity.prisma`, `BotLinkService.hasVerifiedLink` querying a column `db push` had never created) |
| otp-channel-choice | choose how to get the code, turn off sms, only telegram and bale, پیامک رو خاموش کن, فقط تلگرام و بله, انتخاب کانال otp | panel-web -> auth-api -> identity | site-pwa/src/app/(auth)/auth/_hooks/useOtpChannels.ts, txnet-backend/auth-service/src/app/auth/otp/otp-channels.service.ts, txnet-backend/auth-service/src/app/config/env.validation.ts | F-0202 | the switch is `OTP_ALLOWED_CHANNELS` in `.env` (+ the bot token / SMS credentials being present) |
| captcha-verified-state | captcha reset, captcha انگار درست reset نمیشه, میرم صفحه بعدی میام قبلی فلش میاد از اول ولی هنوز رو حال verified هستش, اسلایدر کپچا از اول میاد, کپچا هر ۲ دقیقه ریستارت میشه, اول slide میکشم میپره عقب باید یکم صبر کنم بعدش بکشم اوکی میشه, اولین بار اسلایدر کار نمیکنه, اطلاعات جدید زده شده بعد اینکه کپچا حل شده بود و درخواست otp رفته بود اگه کاربر اطلاعات قبلی رو عوض کرد کپچا باید جدید شه | panel-web -> auth-api -> redis-keyspace | site-pwa/src/app/(auth)/auth/_components/NatureCaptchaUI.tsx, site-pwa/src/app/(auth)/auth/_hooks/useCaptcha.ts, site-pwa/src/app/(auth)/auth/login/page.tsx, site-pwa/src/app/(auth)/auth/register/page.tsx, site-pwa/src/app/(auth)/auth/forgot-password/page.tsx | F-0201 | the pass lives in `useCaptcha` (parent), the thumb position in `NatureCaptchaUI` (child). A multi-step form unmounts the child, so its slider must be seeded from `isVerified`. The 120s re-arm is by design — `RedisTtl.captchaVerified`. Two more failure shapes, both in `useCaptcha`: the challenge is fetched on mount, so a slide that lands before the id does used to return early and snap the thumb back (`complete` now awaits the in-flight request); and the pass is single-use server-side, so every caller must `spend()` it after handing it to an endpoint or a user who goes back and edits step 1 re-submits a burnt token |
| bot-link-webhook | open the bot, waiting for you to confirm in the messenger, چیو باید دوباره اجرا کنم تا وبهوک جدید ست بشه, وبهوک جدید ست نمیشه, وبهوک ست نمیشه, ری استارت کردم ولی وبهوک آپدیت نشد, Connection refused در getWebhookInfo, telegram could not read the current webhook, بات رو باز میکنم هیچی نمیاد, اینجا وایمیسته ولی لینکو که باز میکنم هیچی نمیادش, لینک بات باز میشه ولی کد نمیاد, ربات جواب نمیده, share your number with the bot but nothing happens | panel-web -> auth-api -> messenger | txnet-backend/bot-service/src/app/webhook/bot-webhook.registrar.ts, txnet-backend/messenger/src/lib/telegram-like-bot.client.ts, txnet-backend/messenger/src/lib/bot-client.registry.ts, txnet-backend/auth-service/src/app/automation/bot-integration.directory.ts | F-0202 | the panel only polls `bots/link/status`; everything else happens in the messenger. If the deep link opens the bot and the bot then says nothing, the bot has no webhook — `getWebhookInfo` is the one command that decides it. `bot-service` registers every tenant's webhook on boot (`BotWebhookRegistrar`), one per `BotIntegration` row; since F-066-i there is no by-hand script and no env token to check — a bot with no row, or no vault token, is simply never registered, and `status`/`lastErrorAt` on the row say so. Two separate reachability problems: outgoing calls to api.telegram.org need `TELEGRAM_API_BASE` (proxy), and Telegram's incoming calls time out against this server, so `TELEGRAM_WEBHOOK_PUBLIC_BASE` sends them back through the same proxy. `last_error_message` in `getWebhookInfo` is where that shows up. **A restart that changes nothing is the normal failure here, not a no-op**: `BotWebhookRegistrar` refuses to overwrite a webhook it could not first read, so a broken `TELEGRAM_API_BASE` makes every boot log `could not read the current webhook — not overwriting it` and leave the old registration standing (2026-09-08: the proxy host was written twice — `…/host/api.telegram.org/api.telegram.org` — and answered 404). The proxy takes `/host/<target host>/<path>`, so the two bases are `…/host/api.telegram.org` outgoing and `…/host/api.txnet.cyou` incoming; pointing the incoming one straight at `api.txnet.cyou` gets `Connection refused`, because Telegram cannot reach this host at all — that is what the proxy is for in both directions |
| bot-login-register | چرا کد میاد تو همون چتی که توشم, otp همینجا چه فایده ای داره, لاگین تو ربات چرا انقدر مرحله داره, ورود با یک تپ در ربات, دکمه های ربات کار نمیکنه, دکمه های روبات کار نمکنه برای ثبت نام و لاگین و فراموشی رمز, دکمه های ربات هیچ کاری نمیکنن, tapping the bot buttons does nothing, bot buttons dead, ربات فقط با تایپ جواب میده, میتونی همین سیستم لاگینو برای روبات bale ,telegram که مثل هم کار میکنن بنویسی؟, داخل بات تلگرام از otp sms و otp bale استفاده کنه, داخل bale از otp telegram و otp sms, ثبت نام و لاگین داخل ربات, همون سیستم لاگین پنل توی ربات, login and register inside the bot, cross messenger otp | bot-app -> messenger -> auth-api -> identity -> redis-keyspace | txnet-backend/bot-service/src/app/flows/otp.step.ts, txnet-backend/bot-service/src/app/conversation/router.ts, txnet-backend/auth-service/src/app/auth/bot-link/bot-link.service.ts, txnet-backend/auth-service/src/app/auth/bot-link/bot-session.service.ts, txnet-backend/auth-service/src/app/common/security/service-caller.ts | F-303 | the bot decides nothing: every step is an `auth-api` call carrying `X-Service-Token` (ADR-0011), and the cross-messenger case is the existing `linkRequired` answer rendered as a deep link. Picking the messenger you are already talking to skips the round trip — `link/resolve` + `link/contact` run in place. If a bot auth step is refused for everyone, check that `SERVICE_AUTH_TOKEN` matches on both services before reading any flow. **Taps doing nothing while typing works is not a flow bug — it is the registration**: every screen is an inline keyboard, so a webhook set with `allowed_updates:['message']` delivers text and silently drops every `callback_query` (fixed 2026-09-06; `WEBHOOK_ALLOWED_UPDATES`). It does not self-heal — the URL still matches, so a URL-only check calls it registered forever; `BotWebhookRegistrar` now compares the update types too, and `getWebhookInfo` shows what a bot actually has. **Signing in no longer sends a code at all** (ADR-0012): a contact-verified `LinkedBotAccount` is the credential, and `POST /auth/bots/session` trades it for the ordinary token pair — `user` role only, an allow-list. A code still crosses for register, password reset, and a login to an account this chat does not own |
| bot-account-link | connect telegram to account, share contact with the bot, verify contact belongs to sender, اتصال حساب به ربات, ارسال مخاطب, تایید مالکیت شماره در تلگرام | panel-web -> auth-api -> identity -> redis-keyspace | txnet-backend/auth-service/src/app/auth/bot-link/bot-link.service.ts, txnet-backend/auth-service/src/app/auth/bot-link/bot-link.store.ts, site-pwa/src/app/(auth)/auth/_components/BotLinkStep.tsx | F-0203 | the ownership check is `contact.user_id === message.from.id` plus a phone match; both live in `BotLinkService.handleContact` |
| bot-conversation-ux | ربات انگلیسی حرف میزنه, زبان ربات عوض نمیشه, Here in this chat یعنی چی, ربات به زبان اپ من جواب میده, تغییر زبان در ربات, اصلا ux خوبی نداره سرویس روبات, ux ربات خوب نیست, کاربر تو ربات گیج میشه, روی ux ربات کار کن, ربات گیج کننده است, برگشت به قدم قبل تو ربات نیست, ربات بعد از هر جواب یه جمله میده و هیچ دکمه ای نداره, نمیدونم کجای ثبت نامم, the bot ux is bad, user gets lost in the bot, no way back in the bot, bot dead ends | bot-app -> messenger | txnet-backend/bot-service/src/app/conversation/router.ts, txnet-backend/bot-service/src/app/flows/steps.ts, txnet-backend/bot-service/src/app/flows/views.ts, txnet-backend/messenger/src/lib/renderer.ts, txnet-backend/messenger/src/lib/bot-view.ts, txnet-backend/bot-service/src/app/locale/chat-language.ts, locales/backend/langs/fa/bot.json | F-303 | orientation is not a flow's job and never was: `ConversationRouter.decorate` is the single place that turns whatever a flow returns into a screen a user can place themselves on — a step count and an echo of the answers already given, Back next to Cancel from the second step onwards, and the menu attached to anything that ends a conversation. Two rules do most of the work: a view with words and no choices becomes the `hint` on the screen the user is *actually* on (so an `auth-api` rejection re-asks the question instead of emptying the chat), and free text arriving with no Redis state is answered with `bot.common.expired` rather than a bare menu. Back replays `lastView` from `NavState.history`, so no flow implements it and every §10.4 flow gets it free. If a new screen looks undecorated, it returned `nextState` with a step that is not in `flows/steps.ts`. **A bot answering in the wrong language is not a missing translation**: `ChatLanguage.resolve` picks the chat's own `/lang` choice, then `BOT_DEFAULT_LANGUAGE`, then `DEFAULT_LANGUAGE`, and only then `from.language_code` (ADR-0016, F-046). Until 2026-09-07 the messenger's hint came second and `BOT_DEFAULT_LANGUAGE` reached no container, so an English Telegram app got an English bot on a Persian deployment. If it still happens now, the cause is upstream of the precedence: `DEFAULT_LANGUAGE` is unset or names a language `locale-service` does not serve — the bot logs a warning naming the var when it skips one |
| bot-empty-env-defaults | ربات جواب نمیده, هر آپدیت ۵۰۰ میده, Wrong response from the webhook: 500 Internal Server Error, pending_update_count بالا میره, ERR value is not an integer or out of range, expire با مقدار خالی, command menu published با لیست خالی, BOT_LANG_TTL_SEC اثر نداره, دیفالت env اعمال نمیشه, bot returns 500 on every update, config default is ignored | bot-app -> redis-keyspace | txnet-backend/bot-service/src/app/config/env.validation.ts, txnet-backend/bot-service/src/app/app.module.ts | F-303 | **An empty environment variable is not an unset one.** `docker-compose.main.yml` passes every optional bot variable as `VAR=${VAR:-}`, so inside the container it arrives as `''`. `optional()` in `env.validation.ts` turns that back into `undefined`, but `ConfigService.get` reads the validated env **first and `process.env` second** — so an undefined validated value fell through to the raw `''` and no default, schema's or call site's, ever ran. Two symptoms from one cause: `BOT_LANG_TTL_SEC=''` reached Redis as `expire <key> ''` (`ChatLanguage.chosen`), an error reply, so **every** webhook update answered 500 and `pending_update_count` climbed while `getWebhookInfo` blamed the webhook; and `BOT_COMMAND_LANGS=''` published the command menu in no languages at all (`command menu published ()` in the boot log — the log line names the languages, which is what makes it visible). Fixed 2026-09-08 by `skipProcessEnv: true` in the shared `envConfigOptions`, making the validated env the only source. **The tell**: `docker exec <container> printenv | grep VAR=$` — a key present with an empty value is this bug, and every service reading config the same way has it until it opts in |
| panel-locale-namespace-injection | بعد از لاگین یهو میپره دوباره لاگین, بعد از لاگین برمیگرده به لاگین, قبلش این خطایی که فرستادم میده, locale-store.ts addNamespaces, NamespaceInjector, Cannot update a component while rendering a different component, ارور locale store موقع لاگین, فرم لاگین از اول شروع میشه | panel-web -> auth-api | site-pwa/src/components/NamespaceInjector.tsx | — | `NamespaceInjector` wrote the compiled `auth` namespaces into the locale store **during render**, deliberately — the comment claimed siblings read them on the first paint. They do not: `(auth)/layout.tsx` hands every language's `auth` namespace to `AuthUIProvider` as a prop, and nothing reads that namespace out of the store at all. The write was a real fault, because `addNamespaces` replaces `state.cache` and both the injector itself and its already-rendered ancestor `AuthUIProvider` subscribe to `cache` through `useStore`/`useSyncExternalStore` — so a mid-render write updates components React has already rendered in that pass. Moved into `useEffect` (ref-guarded against Strict Mode's double invoke). Sharp edge: the cookie half of this report was **not** reproduced — if the bounce to `/auth/login` survives the fix it is `PanelSessionProvider`'s catch, i.e. `POST /api/auth/refresh` failing on the panel's first paint, and the cookie attributes are not the cause (`.txnet.cyou` + `SameSite=Lax` is same-site from `panel.`, and dev is HTTPS so `Secure` is satisfied). Diagnosed 2026-09-06; `--walk` sent this sentence to `bot-app`, which is why the aliases are here |
| panel-login-bounce-rotation | بعد از لاگین یهو میپره دوباره لاگین, بعدش که میرم صفحه بعد لاگین یهو ریدارکت میکنه صفحه لاگین, با اینکه کوکی ست هستش, کوکی هست ولی لاگین نمیمونه, بعد از لاگین برمیگرده به لاگین, بعد از رفرش لاگ اوت میشم, panel redirects back to login, cookie is set but the panel bounces me out, logged out right after logging in | panel-web -> auth-api -> identity | site-pwa/src/proxy.ts, txnet-backend/auth-service/src/app/auth/auth.controller.ts, txnet-backend/auth-service/src/app/auth/auth.service.ts | F-0101 | **Do not read this as a cookie problem — the cookie is fine and the user will tell you it is there.** `POST /auth/refresh` rotates: it revokes the session it is handed and mints a replacement. `proxy.ts` used it as the read-only "is this visitor signed in?" probe for F-0101, and its `config.matcher` covers every non-static path — so RSC prefetches of `/auth/login`, redirects and in-flight duplicates each rotated the session and returned the new token in a `Set-Cookie` the browser might never apply. The browser kept a revoked cookie that still shows in devtools, and `PanelSessionProvider`'s `ensureSession()` then failed on the panel's first paint and `replace`d to the login screen. Fixed by splitting the read from the write: `GET /auth/session` (ADR-0013). **The one query that settles it**: `select "issuedAt", "revokedAt", "userAgent" from identity.session order by "issuedAt" desc` — rows with `userAgent = node` were minted by the proxy's server-to-server call and no browser ever held them. A refresh failure logs nothing, because `ok:false` rides inside a 200 envelope; the visible symptom in the auth-service log is the *next* call, `GET /api/auth/accounts -> 401 auth.authorizationRequired` with no Authorization header at all, which is the panel's other components firing while the redirect is still in flight |
| error-display-locale | ببین میخوام تمام خطا درست نمایش بده به کاربر . داخل تمامی سرویس ها با locale درست!, ارور نشون نمیده, خطاها نمایش داده نمیشن, خطا به زبان اشتباه, ارور انگلیسی میاد ولی پنل فارسیه, session_revoked روی صفحه, کلید خام به جای پیام, errors are not shown to the user, error in the wrong language, raw key instead of a message | panel-web -> auth-api -> forward-auth -> bot-app | site-pwa/src/lib/auth-api.ts, site-pwa/src/lib/api-error.ts, site-pwa/src/lib/api-language.ts, site-pwa/src/hooks/useApiError.ts, site-pwa/src/app/(auth)/auth/_components/FormError.tsx, site-pwa/src/app/(auth)/auth/_hooks/useSubmitError.ts, txnet-backend/bot-service/src/app/auth-api/auth-api.client.ts, auth-handler/internal/api/handlers/handler.go, auth-handler/internal/api/middlewares/middleware.go, auth-handler/internal/response/response.go | — | **`auth-api` was never the problem** — `sanitizeError` + `I18nExceptionFilter` translate correctly and always did. Four other things stood between that and the user. (1) The three `panel-web` auth screens ended their `catch` at `console.error`, so a wrong password looked exactly like a form that had stopped; `<FormError>` is the surface they lacked. (2) The browser sends the *OS* language in `Accept-Language`, which is what `auth-api` translates from — a Persian panel on an en-US machine got English back until `LocaleProvider` started mirroring its `lang` into `lib/api-language.ts`. (3) The panel dropped `fieldErrors` entirely. (4) `forward-auth` translated against a namespace called `messages` that `locale-service` does not serve, so **every** gateway message reached the client as a raw snake_case key (`session_revoked`) — it now uses the shared `errors` namespace, and its panic and timeout answers carry the envelope instead of `internal server error` / `{"error":"request timeout"}`. Its `SafeExecute` was also putting the raw Go error text on the wire. The bot had the mirror-image bug: a transport failure returned the *key* `bot.common.tryAgain` into a field every caller renders as `BotText.raw`, i.e. already-translated text. **The rule that ties all five together**: whoever produces a message translates it, and whoever shows it never translates again — so a producer that cannot translate (no answer came back) must say so, which is what `ApiError.unreachable` and the bot's `unreachable()` are for |
