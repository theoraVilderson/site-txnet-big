---
id: identity
layer: domain
updated: 2026-09-10
---

# Open questions — identity

| Date | Question | Blocking? | Current assumption | Exit path |
|---|---|---|---|---|
| 2026-09-04 | `isSystemRole` deletion protection is only a comment, not a DB/service check. Add it? | no | ASSUMED(2026-09-04): enforced in the (unwritten) role-admin service | -> rules.md + constraint |
| 2026-09-04 | `twoFactorEnabled` login path exists but there is no endpoint to enable/manage 2FA or `preferredOtpChannel`. Which unit owns profile settings? | no | ASSUMED(2026-09-04): future `governance` UserSetting or an identity profile endpoint | -> ADR or rules.md |
| 2026-09-04 | No Prisma migration history is committed. Is dev `db push` the intended flow until launch? | resolved | **Answered 2026-09-09 (D-5).** Hand-written `.sql` in `prisma/domains/migrations/`, one history, applied before the first production data — five migrations exist today | -> `docs/operations/migrations.md` |
| 2026-09-04 | Role rank for impersonation is a hard-coded map (`SuperAdmin/Admin/Support/User`) in `impersonation.service.ts`, disconnected from the `role` table. Reconcile? | no | ASSUMED(2026-09-04): seed roles use exactly those names | -> rules.md |
| 2026-09-09 | A surface that is not an HTTP request has no tenant. `bot-service` calls `auth-api` on an internal host, so the resolver sees a container name no `tenant_domain` row matches. Until 2026-09-09 that fell into `DEFAULT_TENANT_SLUG`; **F-066-d removed the fallback (ADR-0025) and bot-service now states its tenant** — `BOT_TENANT_ID` as `X-Tenant-Id`, honoured because its service token verified. One process, one tenant: the collision is *declared* rather than guessed, and F-066-i makes it per-`BotIntegration`. Until a bot request names its tenant, identity cannot be scoped per tenant (ADR-0023) — scoping the panel alone moves the collision rather than removing it | yes (tenancy) | ASSUMED(2026-09-09): the mapping is `tenant_bot_integration`, whose reader does not exist yet (F-018). No narrower stand-in is assumed — the row is `todo`, not built on a guess | -> F-066-i (F-065-a was dropped into it), then ADR-0023's F-065-b / F-066-l |
| 2026-09-10 | **F-067-a: where does the plaintext code exist once the send leaves the request?** The row says "the plaintext code does not ride the queue — the message carries the Redis key the code was already saved under", but the only thing saved under that key is an argon2id hash (`OtpStore.save`), and a sender needs the code itself. Every shape that keeps the draw in the request puts the plaintext somewhere at rest — the queue or a second Redis key — which invariant #2 forbids. The shape that does not is the worker drawing the code | resolved | **Answered 2026-09-10, on the user's call to pick the most principled shape: the draw moved.** The code is created inside the process that sends it and is never written down but as its hash; invariant #2 is unamended and now says so explicitly | -> identity/contract.md v11 + invariant #2 |
| 2026-09-10 | **F-067-a: which process consumes the OTP send?** The senders are auth-service-internal and tenant-scoped: `TelegramOtpSender`/`BaleOtpSender` need `BotLinkStore`, `TenantContext`, `LocaleService` and `BotClientRegistry`; `SmsOtpSender` needs `LocaleService` and the SMS provider config. Only `BotClientRegistry` is already a shared lib. `worker-service` has none of the other three, so "publish the send" is also a decision about where the senders live — and ADR-0027 forbids the request-serving process consuming its own queue | resolved | **Answered 2026-09-10: neither.** `worker-service` consumes and calls `POST /api/internal/otp/deliver`, which is what `vault_credential_retention` already does for the Credential Vault (F-031-c) — the senders do not move, no unit is created, and ADR-0027 is not carved out | -> `domains/automation/contract.worker.md`, `interfaces/auth-api/contract.md` |
| 2026-09-10 | **A switch moves one session; a place now holds two.** Since ADR-0032 a bot chat and its Mini App share one scope and each hold their own session, so `POST /auth/accounts/switch` — which revokes the caller's session and mints one for the target (`switchTo`) — moves one surface and leaves the other signed in as the outgoing account. Observed live: one scope, chat as account A, Mini App as account B. Revoking the sibling session is **not** the fix: the surface that lost its session re-signs itself in against the `LinkedBotAccount`, which ADR-0014 deliberately leaves where it was, so it snaps back to the *linked* account rather than following the switch. Making a switch move the place needs a remembered "acting as" for the scope — the shape ADR-0014 rejected as "a second identity concept owned by `bot-app` that no other surface has", a reason ADR-0015/0032 have since undercut by making the scope itself a first-class shared thing. ADR-0014's own revisit trigger anticipates this | resolved | **Answered 2026-09-10 on the user's call: the place remembers.** `audit.linked_account_group.actingAsUserId` — written by a switch (which also sweeps the outgoing account's other sessions in that scope), read only by an implicit sign-in, and inert when it names someone no longer a member here. `LinkedBotAccount` is untouched, so invariant #12 and the OTP phone->chat lookup stand | -> [ADR-0034](../../architecture/decisions/0034-a-switch-moves-the-place-and-the-place-remembers-it.md) + migration `20260910002000_switch_scope_acting_as` |
| 2026-09-11 | `otp.botLink.{expired,noAccount,phoneMismatch,senderMismatch,takenByAnotherAccount}` is translated twice: in `errors` (shown on the site) and in `notifications` (sent to the chat). The two texts already differ in all five, in both languages — the chat version also says what to do next. One sentence per name, or two audiences on purpose? | no | ASSUMED(2026-09-11): two audiences on purpose; merging would change live copy. `auth-service/src/app/locale/dynamic-keys.spec.ts` pins the *names* in both files so they cannot drift apart, only the sentences | -> a copy decision, then a rule |
