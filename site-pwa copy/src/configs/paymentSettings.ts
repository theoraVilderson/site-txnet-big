export enum FeeMode {
  MANUAL = "MANUAL",
  API = "API",
}
export enum FeeType {
  FIXED = "FIXED",
  PERCENT = "PERCENT",
}
export enum FeeCurrency {
  IRR = "IRR",
  IRT = "IRT",
  USD = "USD",
}
export enum PaymentProvider {
  ZARINPAL = "ZARINPAL",
}
