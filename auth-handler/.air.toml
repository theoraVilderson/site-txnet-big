root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/main ./cmd/server"
bin = "./tmp/main"
include_dir = ["cmd", "internal", "pkg"]
include_ext = ["go"]
exclude_dir = ["tmp", "configs"]
delay = 1000

[misc]
clean_on_exit = true
