---
id: <same-id>
status: draft
updated: YYYY-MM-DD
---

# <Unit> - data model

**Never duplicate the schema.** Link to it and explain only what the schema
cannot express: why a table exists, why a column is nullable, which writes must
share a transaction.

Schema: `<path/to/schema>`

| table | purpose | written together with |
|---|---|---|
